version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
  build:
    commands:
      - npm i
      - ng build --configuration=dev
  cache:
    paths:
      - 'node_modules/**/*'
  artifacts:
    files:
      - 'dist/**/*'
