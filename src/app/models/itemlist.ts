import {Item} from './item';

export class ItemList {
  items: Item[];
  // tslint:disable-next-line:variable-name
  max_pages: number;
  // tslint:disable-next-line:variable-name
  num_items: number;
}
